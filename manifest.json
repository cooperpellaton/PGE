{
  "manifest_version": 2,

  "name": "PGE: Pretty Good Email Protection",
  "description": "This is an open source, completely compatible email encryption plugin to ensure that all of your emails are held to a reasonable expectation of privacy. In order for it to work it will require that the extension is enabled on both the sending and recieving sides.",
  "version": "0.1",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab",
    "https://ajax.googleapis.com/"
  ]
}
